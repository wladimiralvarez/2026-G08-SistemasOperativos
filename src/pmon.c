//monitor de procesos
#include <stdio.h>

#include "pmon.h"

int pmon_run(int seconds)
{
    fprintf(stderr,
            "mishell: pmon todavía no está implementado "
            "(intervalo solicitado: %d s)\n", seconds);
    return 1;
}
